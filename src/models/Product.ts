export interface Product {
  id: string;
  userId: string;
  name: string;
  unit: string;
  costPrice: number;
  sellingPrice: number;
  stockQuantity: number;
  createdAt: number;
  updatedAt: number;
}